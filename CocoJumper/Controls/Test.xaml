<UserControl x:Class="CocoJumper.Controls.Test"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CocoJumper.Controls"
             mc:Ignorable="d" 
             
             d:DesignHeight="17.641" d:DesignWidth="38.692">
    <Grid Background="red" >
        <Grid.RowDefinitions>
            <RowDefinition Height="0*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Txt, Mode=TwoWay}" Grid.RowSpan="2" FontSize="15"/>
    </Grid>
</UserControl>
